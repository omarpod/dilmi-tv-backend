# Django وDjango REST Framework
# ملاحظة: رُفعت من 5.0.6 إلى 5.2 (LTS) لأن django-unfold==0.91.0 يتطلب
# Django>=5.1 صراحة — البقاء على 5.0.6 كان سيُفشل pip install بتعارض
# إصدارات فوراً على Railway. 5.2 مُختارة تحديداً لأنها إصدار LTS طويل
# الدعم (وليس 5.1 العادي قصير الدعم)
Django==5.2.16
djangorestframework==3.16.1
django-cors-headers==4.3.1

# قاعدة بيانات PostgreSQL (Railway) + قراءة DATABASE_URL
dj-database-url==2.2.0
psycopg[binary]==3.2.10

# لوحة تحكم عصرية (Tailwind) — تستبدل django-admin-interface بالكامل
django-unfold==0.91.0

# خدمة الملفات الثابتة في الإنتاج (بدون Nginx منفصل)
whitenoise==6.7.0
gunicorn==22.0.0

# الصور + التخزين الدائم على Cloudinary (Railway لا يحتفظ بالملفات المرفوعة
# بين عمليات النشر — أي تخزين محلي على القرص يُفقد عند كل Deploy جديد)
Pillow==12.3.0
django-cloudinary-storage==0.3.0
cloudinary==1.41.0

# سحب الأخبار من RSS (يُستدعى من sync_data.py)
feedparser==6.0.11
requests==2.32.3

# إشعارات Push للتطبيق (Firebase Cloud Messaging) — عند بدء مباراة مباشرة
# أو عند نشر تحديث جديد للتطبيق
firebase-admin==6.6.0

# Celery + Redis: طوابير المهام (مزامنة دورية + فحص صحة روابط البث) —
# بديل لحلقة run_sync_loop اليدوية، وdjango-redis يُستخدم أيضاً كذاكرة
# تخزين مؤقت مشتركة بين كل عمليات gunicorn (بدل LocMemCache لكل عملية)
celery==5.4.0
redis==5.0.8
django-redis==5.4.0

# تحليل تواريخ/أوقات مرنة الصيغة (استيراد جدول المباريات الجماعي من ملف
# CSV/JSON يرفعه المستخدم يدوياً — راجع apps/core/bulk_import.py) — لا
# يمكن التحكم بالصيغة التي سيلصقها المستخدم من Excel/Sheets يدوياً
python-dateutil==2.9.0
