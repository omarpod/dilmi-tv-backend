# Django وDjango REST Framework
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1

# قاعدة بيانات PostgreSQL (Railway) + قراءة DATABASE_URL
dj-database-url==2.2.0
psycopg[binary]==3.2.10

# لوحة تحكم احترافية قابلة للتخصيص من الواجهة نفسها (ألوان، شعار)
django-admin-interface==0.29.4
django-colorfield==0.11.0

# خدمة الملفات الثابتة في الإنتاج (بدون Nginx منفصل)
whitenoise==6.7.0
gunicorn==22.0.0

# الصور + التخزين الدائم على Cloudinary (Railway لا يحتفظ بالملفات المرفوعة
# بين عمليات النشر — أي تخزين محلي على القرص يُفقد عند كل Deploy جديد)
Pillow==12.3.0
django-cloudinary-storage==0.3.0
cloudinary==1.41.0

# سحب الأخبار من RSS (يُستدعى من sync_data.py)
feedparser==6.0.11
requests==2.32.3

# إشعارات Push للتطبيق (Firebase Cloud Messaging) — عند بدء مباراة مباشرة
# أو عند نشر تحديث جديد للتطبيق
firebase-admin==6.6.0
